Функции API терминала:
term.write( text )
term.blit( text, textColor, backgroundColor )
term.clear()
term.clearLine()
term.getCursorPos()
term.setCursorPos( x, y )
term.setCursorBlink( blink )
term.isColor()
term.setTextColor( color )
term.setBackgroundColor( color )
term.getTextColor()
term.getBackgroundColor()
term.getSize()
term.scroll( n )
term.redirect( object )
term.current()
term.setPaletteColor( color, r, g, b )
term.getPaletteColor( color )

События, создаваемые терминалами:
"term_resize" — при изменении размера терминала. Это возможно в многозадачной среде или при перенаправлении вывода терминала программой "monitor".
