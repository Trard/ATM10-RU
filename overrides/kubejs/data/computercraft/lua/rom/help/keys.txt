API keys содержит константы для всех кодов клавиш, которые может вернуть событие "key":

Пример использования:
local sEvent, nKey = os.pullEvent()
if sEvent == "key" and nKey == keys.enter then
   -- Выполнить действие
end

Полный список приведён на https://www.minecraft.wiki/w/Key_codes и в исходном коде.
